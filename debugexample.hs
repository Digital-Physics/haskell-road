import Debug.Trace
-- has side effects by printing out, so not really functional programming, but ok for debugging

-- random line of Florian's code:
-- matchingSum xs ys = sum . map fst . filter (uncurry (==)) $ zip xs ys

-- print x = putStrLn (show x)
